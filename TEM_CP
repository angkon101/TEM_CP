                //           IUBAT_DBug
                //           CSE,IUBAT
#include <bits/stdc++.h>
#include<string>
using namespace std;

#define fst_io ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define ll                         long long
#define ull                        unsigned long long
#define nl                         endl
#define N                          1e6
#define mini3(a,b,c)                min(a,min(b,c))
#define mini4(a,b,c,d)              min(a,min(b,min(c,d)))
#define maxi3(a,b,c)                max(a,max(b,c))
#define maxi4(a,b,c,d)              max(a,max(b,max(c,d)))
#define arrsort                     sort(arr, arr+n)
#define arrinput                    for(int i=0; i<n; i++){ cin>>arr[i];}
#define arroutput                   for(int i=0; i<n; i++){cout<<arr[i];}
void IUBAT_DBug() {   }
int main(){
    fst_io
    // int t; cin>>t;
    // for(int i=0; i<t; i++)
    // {
    //     IUBAT_DBug();
    // }
    // while(t--) 
    IUBAT_DBug();
}
